function iconActivity() {
  return `
    <svg viewBox="0 0 24 24" class="icon" fill="none" aria-hidden="true">
      <path d="M4 12h4l2-6 4 12 2-6h4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
  `;
}

function iconServer() {
  return `
    <svg viewBox="0 0 24 24" class="icon" fill="none" aria-hidden="true">
      <path d="M6.5 4h11A2.5 2.5 0 0 1 20 6.5v1A2.5 2.5 0 0 1 17.5 10h-11A2.5 2.5 0 0 1 4 7.5v-1A2.5 2.5 0 0 1 6.5 4Z" stroke="currentColor" stroke-width="1.8"/>
      <path d="M6.5 14h11A2.5 2.5 0 0 1 20 16.5v1A2.5 2.5 0 0 1 17.5 20h-11A2.5 2.5 0 0 1 4 17.5v-1A2.5 2.5 0 0 1 6.5 14Z" stroke="currentColor" stroke-width="1.8"/>
      <path d="M7.5 7h.01M7.5 17h.01M11 7h6M11 17h6" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
    </svg>
  `;
}

function iconShield() {
  return `
    <svg viewBox="0 0 24 24" class="icon" fill="none" aria-hidden="true">
      <path d="M12 21s7-3.5 7-10V5.8c0-.62-.38-1.17-.95-1.39l-5.5-2.12a1.5 1.5 0 0 0-1.1 0l-5.5 2.12C5.38 4.63 5 5.18 5 5.8V11c0 6.5 7 10 7 10Z" stroke="currentColor" stroke-width="1.8"/>
      <path d="m9.5 11.8 1.7 1.7 3.6-4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
  `;
}

function iconCloud() {
  return `
    <svg viewBox="0 0 24 24" class="icon" fill="none" aria-hidden="true">
      <path d="M8 18h9a4 4 0 0 0 .6-7.95A6 6 0 0 0 6.3 8.2 4.8 4.8 0 0 0 8 18Z" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
  `;
}

function iconTrash() {
  return `
    <svg viewBox="0 0 24 24" class="icon" fill="none" aria-hidden="true">
      <path d="M5 7h14" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
      <path d="M10 11v6M14 11v6" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
      <path d="M8 7l.5-2h7L16 7" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
      <path d="M7 7l.8 13h8.4L17 7" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/>
    </svg>
  `;
}

function iconCamera() {
  return `
    <svg viewBox="0 0 24 24" class="icon" fill="none" aria-hidden="true">
      <path d="M4 8.5C4 7.12 5.12 6 6.5 6h1.17c.53 0 1.04-.21 1.41-.59l.83-.82c.37-.38.88-.59 1.41-.59h1.36c.53 0 1.04.21 1.41.59l.83.82c.37.38.88.59 1.41.59h1.17C18.88 6 20 7.12 20 8.5v7A2.5 2.5 0 0 1 17.5 18h-11A2.5 2.5 0 0 1 4 15.5v-7Z" stroke="currentColor" stroke-width="1.8"/>
      <path d="M12 15a3 3 0 1 0 0-6 3 3 0 0 0 0 6Z" stroke="currentColor" stroke-width="1.8"/>
    </svg>
  `;
}

function iconGrid() {
  return `
    <svg viewBox="0 0 24 24" class="icon" fill="none" aria-hidden="true">
      <path d="M4 5.5A1.5 1.5 0 0 1 5.5 4h4A1.5 1.5 0 0 1 11 5.5v4A1.5 1.5 0 0 1 9.5 11h-4A1.5 1.5 0 0 1 4 9.5v-4ZM13 5.5A1.5 1.5 0 0 1 14.5 4h4A1.5 1.5 0 0 1 20 5.5v4a1.5 1.5 0 0 1-1.5 1.5h-4A1.5 1.5 0 0 1 13 9.5v-4ZM4 14.5A1.5 1.5 0 0 1 5.5 13h4a1.5 1.5 0 0 1 1.5 1.5v4A1.5 1.5 0 0 1 9.5 20h-4A1.5 1.5 0 0 1 4 18.5v-4ZM13 14.5a1.5 1.5 0 0 1 1.5-1.5h4a1.5 1.5 0 0 1 1.5 1.5v4a1.5 1.5 0 0 1-1.5 1.5h-4a1.5 1.5 0 0 1-1.5-1.5v-4Z" stroke="currentColor" stroke-width="1.8"/>
    </svg>
  `;
}

function iconUpload() {
  return `
    <svg viewBox="0 0 24 24" class="icon" fill="none" aria-hidden="true">
      <path d="M12 15V4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
      <path d="m7.5 8.5 4.5-4.5 4.5 4.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
      <path d="M5 15v2.5A2.5 2.5 0 0 0 7.5 20h9A2.5 2.5 0 0 0 19 17.5V15" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
    </svg>
  `;
}

function iconChart() {
  return `
    <svg viewBox="0 0 24 24" class="icon" fill="none" aria-hidden="true">
      <path d="M5 19V5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
      <path d="M5 19h14" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
      <path d="M8.5 16v-4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
      <path d="M12 16V8" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
      <path d="M15.5 16v-6" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
    </svg>
  `;
}

function iconDocs() {
  return `
    <svg viewBox="0 0 24 24" class="icon" fill="none" aria-hidden="true">
      <path d="M7 3.5h7l4 4V20a1.5 1.5 0 0 1-1.5 1.5h-9A1.5 1.5 0 0 1 6 20V5A1.5 1.5 0 0 1 7.5 3.5Z" stroke="currentColor" stroke-width="1.8"/>
      <path d="M14 3.5V8h4.5" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/>
      <path d="M9 13h6M9 16h6M9 10h2" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
    </svg>
  `;
}

function iconSettings() {
  return `
    <svg viewBox="0 0 24 24" class="icon" fill="none" aria-hidden="true">
      <path d="M12 15.2a3.2 3.2 0 1 0 0-6.4 3.2 3.2 0 0 0 0 6.4Z" stroke="currentColor" stroke-width="1.8"/>
      <path d="M19.4 13.5c.08-.49.1-.99.05-1.5.05-.51.03-1.01-.05-1.5l1.55-1.2-1.9-3.3-1.85.75a7.4 7.4 0 0 0-2.6-1.5L14.35 3h-3.8l-.25 2.25a7.4 7.4 0 0 0-2.6 1.5L5.85 6l-1.9 3.3 1.55 1.2A7.8 7.8 0 0 0 5.45 12c-.05.51-.03 1.01.05 1.5l-1.55 1.2 1.9 3.3 1.85-.75a7.4 7.4 0 0 0 2.6 1.5l.25 2.25h3.8l.25-2.25a7.4 7.4 0 0 0 2.6-1.5l1.85.75 1.9-3.3-1.55-1.2Z" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/>
    </svg>
  `;
}

function iconMoon() {
  return `
    <svg class="theme-icon theme-icon-dark" viewBox="0 0 24 24" fill="none" aria-hidden="true">
      <path d="M21 14.2A7.8 7.8 0 0 1 9.8 3 8.8 8.8 0 1 0 21 14.2Z" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/>
    </svg>
  `;
}

function iconSun() {
  return `
    <svg class="theme-icon theme-icon-light" viewBox="0 0 24 24" fill="none" aria-hidden="true">
      <path d="M12 4V2.5M12 21.5V20M4 12H2.5M21.5 12H20M5.6 5.6 4.5 4.5M19.5 19.5l-1.1-1.1M18.4 5.6l1.1-1.1M4.5 19.5l1.1-1.1" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
      <path d="M12 16a4 4 0 1 0 0-8 4 4 0 0 0 0 8Z" stroke="currentColor" stroke-width="1.8"/>
    </svg>
  `;
}

function statusView({ config = {}, server = {} }) {
  const storageReady =
    Boolean(config.cloudNameReady) &&
    Boolean(config.apiKeyReady) &&
    Boolean(config.apiSecretReady);

  const accessReady = Boolean(config.deviceKeyReady);
  const systemReady = Boolean(server.uptime);
  const uploadReady = storageReady && accessReady;

  return `
<!DOCTYPE html>
<html lang="id" data-theme="light">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>Settings · Capture Panel</title>
  <link
    rel="icon"
    type="image/svg+xml"
    href="/favicon.svg"
  />

  <script>
    (function () {
      const savedTheme = localStorage.getItem("capture-panel-theme");
      const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
      const theme = savedTheme || (prefersDark ? "dark" : "light");

      document.documentElement.setAttribute("data-theme", theme);
    })();
  </script>

  <style>
    :root {
      --bg: #f5f7fb;
      --surface: #ffffff;
      --surface-soft: #f8fafc;
      --text: #111827;
      --text-strong: #0f172a;
      --muted: #6b7280;
      --muted-2: #9ca3af;
      --line: #e5e7eb;
      --line-strong: #d1d5db;
      --primary: #111827;
      --primary-text: #ffffff;
      --accent: #0f766e;
      --accent-soft: #ecfdf5;
      --danger: #dc2626;
      --danger-soft: #fef2f2;
      --warning: #b45309;
      --warning-soft: #fffbeb;
      --radius-xl: 28px;
      --radius-lg: 20px;
      --shadow: 0 20px 60px rgba(15, 23, 42, 0.08);
      --sidebar: #0b1220;
      --sidebar-soft: rgba(255, 255, 255, 0.08);
      --sidebar-line: rgba(255, 255, 255, 0.10);
    }

    html[data-theme="dark"] {
      --bg: #020617;
      --surface: #0f172a;
      --surface-soft: #111827;
      --text: #e5e7eb;
      --text-strong: #f8fafc;
      --muted: #94a3b8;
      --muted-2: #64748b;
      --line: rgba(255, 255, 255, 0.09);
      --line-strong: rgba(255, 255, 255, 0.16);
      --primary: #2dd4bf;
      --primary-text: #020617;
      --accent: #2dd4bf;
      --accent-soft: rgba(45, 212, 191, 0.12);
      --danger: #fb7185;
      --danger-soft: rgba(251, 113, 133, 0.14);
      --warning: #fbbf24;
      --warning-soft: rgba(251, 191, 36, 0.12);
      --shadow: 0 20px 60px rgba(0, 0, 0, 0.28);
      --sidebar: #030712;
      --sidebar-soft: rgba(255, 255, 255, 0.09);
      --sidebar-line: rgba(255, 255, 255, 0.10);
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      min-height: 100vh;
      color: var(--text);
      background: var(--bg);
      font-family:
        Inter,
        ui-sans-serif,
        system-ui,
        -apple-system,
        BlinkMacSystemFont,
        "Segoe UI",
        sans-serif;
    }

    a {
      color: inherit;
    }

    .app {
      min-height: 100vh;
      display: grid;
      grid-template-columns: 270px minmax(0, 1fr);
    }

    .sidebar {
      position: sticky;
      top: 0;
      height: 100vh;
      padding: 22px;
      background:
        radial-gradient(circle at top left, rgba(20, 184, 166, 0.18), transparent 34%),
        var(--sidebar);
      color: #ffffff;
      border-right: 1px solid var(--sidebar-line);
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 28px;
    }

    .brand-mark {
      width: 42px;
      height: 42px;
      display: grid;
      place-items: center;
      border-radius: 14px;
      background: #ffffff;
      color: #0b1220;
    }

    html[data-theme="dark"] .brand-mark {
      background: rgba(45, 212, 191, 0.12);
      color: #5eead4;
      border: 1px solid rgba(45, 212, 191, 0.18);
    }

    .brand-title {
      font-size: 15px;
      font-weight: 800;
      letter-spacing: -0.02em;
    }

    .brand-subtitle {
      margin-top: 2px;
      color: rgba(255, 255, 255, 0.55);
      font-size: 12px;
    }

    .nav {
      display: grid;
      gap: 6px;
    }

    .nav a {
      display: flex;
      align-items: center;
      gap: 11px;
      min-height: 42px;
      padding: 0 12px;
      border-radius: 13px;
      text-decoration: none;
      color: rgba(255, 255, 255, 0.68);
      font-size: 14px;
      font-weight: 650;
      transition: 0.18s ease;
    }

    .nav a:hover,
    .nav a.active {
      color: #ffffff;
      background: var(--sidebar-soft);
    }

    .sidebar-footer {
      position: absolute;
      left: 22px;
      right: 22px;
      bottom: 22px;
      padding: 14px;
      border-radius: 18px;
      background: rgba(255, 255, 255, 0.07);
      border: 1px solid var(--sidebar-line);
    }

    .sidebar-footer span {
      display: block;
      color: rgba(255, 255, 255, 0.56);
      font-size: 12px;
      margin-bottom: 6px;
    }

    .sidebar-footer strong {
      display: block;
      font-size: 13px;
      line-height: 1.4;
      word-break: break-word;
    }

    .main {
      min-width: 0;
      padding: 26px;
    }

    .topbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 18px;
      margin-bottom: 22px;
    }

    .page-title h1 {
      margin: 0;
      font-size: 28px;
      letter-spacing: -0.045em;
      color: var(--text-strong);
    }

    .page-title p {
      margin: 6px 0 0;
      color: var(--muted);
      font-size: 14px;
    }

    .top-actions {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      justify-content: flex-end;
    }

    .btn,
    .theme-toggle {
      min-height: 42px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      padding: 0 14px;
      border-radius: 13px;
      border: 1px solid var(--line-strong);
      background: var(--surface);
      color: var(--text);
      text-decoration: none;
      font-size: 14px;
      font-weight: 750;
      box-shadow: 0 8px 18px rgba(15, 23, 42, 0.04);
      transition: 0.18s ease;
    }

    .btn:hover,
    .theme-toggle:hover {
      transform: translateY(-1px);
    }

    .btn-primary {
      color: var(--primary-text);
      background: var(--primary);
      border-color: var(--primary);
    }

    .theme-toggle {
      width: 42px;
      padding: 0;
      cursor: pointer;
    }

    .theme-icon {
      width: 18px;
      height: 18px;
      display: none;
    }

    html[data-theme="light"] .theme-icon-dark {
      display: block;
    }

    html[data-theme="dark"] .theme-icon-light {
      display: block;
    }

    .icon {
      width: 20px;
      height: 20px;
      display: block;
    }

    .metrics {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 16px;
      margin-bottom: 16px;
    }

    .metric-card {
      padding: 18px;
      border-radius: var(--radius-lg);
      background: var(--surface);
      border: 1px solid var(--line);
      box-shadow: 0 10px 28px rgba(15, 23, 42, 0.04);
    }

    .metric-top {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 12px;
      margin-bottom: 18px;
    }

    .metric-label {
      color: var(--muted);
      font-size: 13px;
      font-weight: 650;
    }

    .metric-icon {
      width: 38px;
      height: 38px;
      display: grid;
      place-items: center;
      border-radius: 13px;
      color: var(--accent);
      background: var(--accent-soft);
    }

    .metric-value {
      margin: 0;
      font-size: 26px;
      line-height: 1;
      letter-spacing: -0.04em;
      color: var(--text-strong);
      word-break: break-word;
    }

    .metric-note {
      margin-top: 8px;
      color: var(--muted-2);
      font-size: 12px;
    }

    .content-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.15fr) minmax(340px, 0.85fr);
      gap: 16px;
    }

    .panel {
      background: var(--surface);
      border: 1px solid var(--line);
      border-radius: var(--radius-xl);
      box-shadow: var(--shadow);
      overflow: hidden;
    }

    .panel-head {
      padding: 18px 20px;
      border-bottom: 1px solid var(--line);
    }

    .panel-head h2 {
      margin: 0;
      color: var(--text-strong);
      font-size: 17px;
      letter-spacing: -0.03em;
    }

    .panel-head p {
      margin: 4px 0 0;
      color: var(--muted);
      font-size: 13px;
    }

    .status-list {
      display: grid;
      gap: 10px;
      padding: 16px;
    }

    .status-row {
      display: grid;
      grid-template-columns: 48px minmax(0, 1fr) auto;
      align-items: center;
      gap: 12px;
      padding: 14px;
      border-radius: 18px;
      background: var(--surface-soft);
      border: 1px solid var(--line);
    }

    .status-icon {
      width: 42px;
      height: 42px;
      display: grid;
      place-items: center;
      border-radius: 14px;
      color: var(--accent);
      background: var(--accent-soft);
    }

    .status-title {
      color: var(--text-strong);
      font-size: 14px;
      font-weight: 850;
    }

    .status-desc {
      margin-top: 3px;
      color: var(--muted);
      font-size: 13px;
      line-height: 1.5;
    }

    .badge {
      width: fit-content;
      padding: 6px 10px;
      border-radius: 999px;
      font-size: 12px;
      font-weight: 850;
      white-space: nowrap;
    }

    .badge-ok {
      color: #047857;
      background: var(--accent-soft);
    }

    html[data-theme="dark"] .badge-ok {
      color: #5eead4;
    }

    .badge-bad {
      color: var(--danger);
      background: var(--danger-soft);
    }

    .badge-warn {
      color: var(--warning);
      background: var(--warning-soft);
    }

    .side-stack {
      display: grid;
      gap: 16px;
    }

    .mini-panel {
      padding: 20px;
    }

    .mini-panel h2 {
      margin: 0;
      color: var(--text-strong);
      font-size: 17px;
      letter-spacing: -0.03em;
    }

    .mini-panel p {
      margin: 8px 0 0;
      color: var(--muted);
      line-height: 1.6;
      font-size: 13px;
    }

    .clear-box {
      display: grid;
      gap: 10px;
      margin-top: 14px;
    }

    .clear-box input {
      width: 100%;
      min-height: 42px;
      padding: 10px 12px;
      border-radius: 13px;
      border: 1px solid var(--line-strong);
      background: var(--surface-soft);
      color: var(--text);
      outline: none;
      font: inherit;
      font-size: 14px;
    }

    .clear-box input:focus {
      border-color: var(--danger);
      box-shadow: 0 0 0 4px rgba(251, 113, 133, 0.14);
    }

    .danger-btn {
      min-height: 42px;
      border: none;
      border-radius: 13px;
      color: #ffffff;
      background: var(--danger);
      font: inherit;
      font-size: 14px;
      font-weight: 850;
      cursor: pointer;
      transition: 0.18s ease;
    }

    .danger-btn:hover {
      transform: translateY(-1px);
    }

    .danger-btn:disabled {
      opacity: 0.65;
      cursor: not-allowed;
      transform: none;
    }

    .clear-status {
      display: none;
      margin-top: 12px;
      padding: 10px 12px;
      border-radius: 13px;
      font-size: 13px;
      line-height: 1.5;
    }

    .clear-status.success {
      display: block;
      color: var(--accent);
      background: var(--accent-soft);
      border: 1px solid var(--line);
    }

    .clear-status.error {
      display: block;
      color: var(--danger);
      background: var(--danger-soft);
      border: 1px solid var(--line);
    }

    code {
      padding: 3px 7px;
      border-radius: 7px;
      color: var(--accent);
      background: var(--accent-soft);
      font-size: 13px;
    }

    .footer-note {
      margin-top: 18px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 14px;
      color: var(--muted-2);
      font-size: 12px;
      flex-wrap: wrap;
    }

    .footer-note strong {
      color: var(--muted);
      font-weight: 750;
    }

    @media (max-width: 1120px) {
      .app {
        grid-template-columns: 1fr;
      }

      .sidebar {
        position: static;
        height: auto;
        border-right: none;
      }

      .sidebar-footer {
        position: static;
        margin-top: 18px;
      }

      .main {
        padding: 20px;
      }

      .metrics,
      .content-grid {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 720px) {
      .topbar {
        align-items: flex-start;
        flex-direction: column;
      }

      .top-actions {
        justify-content: flex-start;
      }

      .status-row {
        grid-template-columns: 1fr;
        align-items: start;
      }

      .sidebar {
        padding: 18px;
      }

      .main {
        padding: 14px;
      }
    }
  </style>
</head>

<body>
  <div class="app">
    <aside class="sidebar">
      <div class="brand">
        <div class="brand-mark">
          ${iconCamera()}
        </div>

        <div>
          <div class="brand-title">Capture Panel</div>
          <div class="brand-subtitle">ESP32-CAM Dataset</div>
        </div>
      </div>

      <nav class="nav">
        <a href="/">${iconGrid()} Dashboard</a>
        <a href="/gallery">${iconCamera()} Gallery</a>
        <a href="/summary">${iconChart()} Summary</a>
        <a href="/test-upload">${iconUpload()} Upload</a>
        <a href="/docs">${iconDocs()} Docs</a>
        <a class="active" href="/status">${iconSettings()} Settings</a>
      </nav>

      <div class="sidebar-footer">
        <span>Current page</span>
        <strong>Settings</strong>
      </div>
    </aside>

    <main class="main">
      <header class="topbar">
        <div class="page-title">
          <h1>Settings</h1>
          <p>Status sistem pengumpulan citra.</p>
        </div>

        <div class="top-actions">
          <button class="theme-toggle" id="themeToggle" type="button" aria-label="Toggle dark mode">
            ${iconMoon()}
            ${iconSun()}
          </button>

          <a class="btn" href="/gallery">${iconCamera()} Gallery</a>
          <a class="btn btn-primary" href="/test-upload">${iconUpload()} Test upload</a>
        </div>
      </header>

      <section class="metrics">
        <article class="metric-card">
          <div class="metric-top">
            <div class="metric-label">System</div>
            <div class="metric-icon">${iconServer()}</div>
          </div>

          <h2 class="metric-value">${systemReady ? "Online" : "Offline"}</h2>
          <div class="metric-note">Panel dapat diakses</div>
        </article>

        <article class="metric-card">
          <div class="metric-top">
            <div class="metric-label">Storage</div>
            <div class="metric-icon">${iconCloud()}</div>
          </div>

          <h2 class="metric-value">${storageReady ? "Ready" : "Check"}</h2>
          <div class="metric-note">Penyimpanan gambar</div>
        </article>

        <article class="metric-card">
          <div class="metric-top">
            <div class="metric-label">Access</div>
            <div class="metric-icon">${iconShield()}</div>
          </div>

          <h2 class="metric-value">${accessReady ? "Ready" : "Missing"}</h2>
          <div class="metric-note">Kunci upload perangkat</div>
        </article>

        <article class="metric-card">
          <div class="metric-top">
            <div class="metric-label">Uptime</div>
            <div class="metric-icon">${iconActivity()}</div>
          </div>

          <h2 class="metric-value" style="font-size: 24px;">${server.uptime || "-"}</h2>
          <div class="metric-note">Durasi server berjalan</div>
        </article>
      </section>

      <section class="content-grid">
        <div class="panel">
          <div class="panel-head">
            <h2>Service status</h2>
            <p>Ringkasan kesiapan sistem.</p>
          </div>

          <div class="status-list">
            <div class="status-row">
              <div class="status-icon">${iconServer()}</div>
              <div>
                <div class="status-title">Backend service</div>
                <div class="status-desc">Server sedang berjalan dan halaman dapat diakses.</div>
              </div>
              <span class="badge ${systemReady ? "badge-ok" : "badge-bad"}">
                ${systemReady ? "Online" : "Offline"}
              </span>
            </div>

            <div class="status-row">
              <div class="status-icon">${iconCloud()}</div>
              <div>
                <div class="status-title">Image storage</div>
                <div class="status-desc">
                  ${
                    storageReady
                      ? "Penyimpanan gambar sudah siap digunakan."
                      : "Penyimpanan gambar belum siap. Upload belum dapat dipastikan berhasil."
                  }
                </div>
              </div>
              <span class="badge ${storageReady ? "badge-ok" : "badge-warn"}">
                ${storageReady ? "Ready" : "Check"}
              </span>
            </div>

            <div class="status-row">
              <div class="status-icon">${iconShield()}</div>
              <div>
                <div class="status-title">Upload access</div>
                <div class="status-desc">
                  ${
                    accessReady
                      ? "Akses upload perangkat sudah tersedia."
                      : "Akses upload belum tersedia. Upload dari perangkat akan ditolak."
                  }
                </div>
              </div>
              <span class="badge ${accessReady ? "badge-ok" : "badge-bad"}">
                ${accessReady ? "Ready" : "Missing"}
              </span>
            </div>

            <div class="status-row">
              <div class="status-icon">${iconUpload()}</div>
              <div>
                <div class="status-title">Upload readiness</div>
                <div class="status-desc">
                  ${
                    uploadReady
                      ? "Sistem siap menerima gambar dari upload manual atau ESP32-CAM."
                      : "Lengkapi status penyimpanan dan akses sebelum pengambilan data."
                  }
                </div>
              </div>
              <span class="badge ${uploadReady ? "badge-ok" : "badge-warn"}">
                ${uploadReady ? "Ready" : "Pending"}
              </span>
            </div>
          </div>
        </div>

        <aside class="side-stack">
          <div class="panel mini-panel">
            <h2>Upload check</h2>
            <p>
              Gunakan test upload untuk memastikan gambar berhasil masuk ke dataset sebelum perangkat digunakan.
            </p>
          </div>

          <div class="panel mini-panel">
            <h2>Local records</h2>
            <p>
              Bersihkan catatan upload lokal jika ingin memulai sesi pengambilan data baru.
            </p>

            <div class="clear-box">
              <input
                id="clearDeviceKey"
                type="password"
                placeholder="Device key"
                autocomplete="off"
              />

              <button id="clearBtn" class="danger-btn" type="button">
                Clear local records
              </button>
            </div>

            <div id="clearStatus" class="clear-status"></div>
          </div>

          <div class="panel mini-panel">
            <h2>Current session</h2>
            <p>
              Status ini mengikuti konfigurasi server yang sedang berjalan saat ini.
            </p>
          </div>
        </aside>
      </section>

      <div class="footer-note">
        <span>Prototype backend · settings</span>
        <strong>© ${new Date().getFullYear()} Reni Kartika Suwandi. All rights reserved.</strong>
      </div>
    </main>
  </div>

  <script>
    const root = document.documentElement;
    const themeToggle = document.getElementById("themeToggle");

    themeToggle?.addEventListener("click", () => {
      const currentTheme = root.getAttribute("data-theme");
      const nextTheme = currentTheme === "dark" ? "light" : "dark";

      root.setAttribute("data-theme", nextTheme);
      localStorage.setItem("capture-panel-theme", nextTheme);
    });

    const clearBtn = document.getElementById("clearBtn");
    const clearDeviceKey = document.getElementById("clearDeviceKey");
    const clearStatus = document.getElementById("clearStatus");

    clearBtn?.addEventListener("click", async () => {
      const deviceKey = clearDeviceKey.value.trim();

      if (!deviceKey) {
        clearStatus.className = "clear-status error";
        clearStatus.textContent = "Device key wajib diisi.";
        return;
      }

      const confirmed = confirm("Hapus semua catatan upload lokal?");

      if (!confirmed) {
        return;
      }

      clearBtn.disabled = true;
      clearBtn.textContent = "Clearing...";
      clearStatus.className = "clear-status";
      clearStatus.textContent = "";

      try {
        const response = await fetch("/api/clear-uploads", {
          method: "POST",
          headers: {
            "x-device-key": deviceKey,
          },
        });

        const data = await response.json();

        if (!response.ok || !data.success) {
          clearStatus.className = "clear-status error";
          clearStatus.textContent = data.message || "Gagal membersihkan catatan upload.";
          return;
        }

        clearStatus.className = "clear-status success";
        clearStatus.textContent = "Catatan upload berhasil dibersihkan.";

        setTimeout(() => {
          window.location.reload();
        }, 900);
      } catch (error) {
        clearStatus.className = "clear-status error";
        clearStatus.textContent = "Request gagal. Server tidak merespons.";
      } finally {
        clearBtn.disabled = false;
        clearBtn.textContent = "Clear local records";
      }
    });
  </script>
</body>
</html>
`;
}

module.exports = statusView;